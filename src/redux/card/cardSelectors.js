export const selectAllCards = state => state.card.cards;

export const selectUpdatingCard = state => state.card.updatingCard;
export const selectUpdatingCardId = state => state.card.updatingCard._id;
